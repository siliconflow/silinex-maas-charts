Logto has been deployed.

App endpoint:
  {{ default (include "logto.endpoint" .) .Values.logto.endpoint }}

Admin endpoint:
  {{ default (include "logto.adminEndpoint" .) .Values.logto.adminEndpoint }}

Check pods:
  kubectl get pods -l app.kubernetes.io/instance={{ .Release.Name }}

Install example:
  helm install {{ .Release.Name }} ./logto
