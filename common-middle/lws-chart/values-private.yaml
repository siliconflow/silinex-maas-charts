# Private registry values for LWS
# This chart now uses global.imageRegistry as the image prefix.

global:
  imageRegistry: registry.inner.silinex.work/silinex-maas

image:
  manager:
    repository: lws
    tag: v0.8.0
    pullPolicy: IfNotPresent
